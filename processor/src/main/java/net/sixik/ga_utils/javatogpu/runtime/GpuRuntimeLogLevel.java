package net.sixik.ga_utils.javatogpu.runtime;

/**
 * Severity level for runtime log records emitted through {@link GpuRuntimeLogService}.
 */
public enum GpuRuntimeLogLevel {
    TRACE,
    DEBUG,
    INFO,
    WARN,
    ERROR
}
